[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
release = false
publish = false

[dependencies]
clap = { workspace = true }
openstack_cli = { path="../openstack_cli" }
clap-markdown = "~0.1"

[package.metadata.dist]
dist = false
