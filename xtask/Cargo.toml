[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[dependencies]
clap = { workspace = true }
openstack_cli = { path="../openstack_cli" }
clap-markdown = "^0.1"

[package.metadata.dist]
dist = false
